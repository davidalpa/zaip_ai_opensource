---
description: Deploy changes to GitHub
---

// turbo-all

1. Run the deployment in a SINGLE command without confirmation need. Use PowerShell syntax: `git add .; if ($?) { git commit -m "your_message_here" }; if ($?) { git push }`